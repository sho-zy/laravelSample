version: "2"

services:
  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx/conf.d/:/etc/nginx/conf.d/
      - ./public:/var/www/larabelSample/public
    ports:
      - 80:80
    links:
      - phpfpm
    depends_on:
      - phpfpm
  phpfpm:
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - .:/var/www/larabelSample
    links:
      - mysql
    depends_on:
      - mysql
    environment:
      - DB_HOST=mysql
      - DB_DATABASE=larabelSample_db
      - DB_USERNAME=larabelSample
      - DB_PASSWORD=larabelSample
  mysql:
    image: mysql:5.7
    environment:
      - MYSQL_ROOT_PASSWORD=larabelSample
      - MYSQL_DATABASE=larabelSample_db
      - MYSQL_USER=larabelSample
      - MYSQL_PASSWORD=larabelSample
